server:
  port: 8021

spring:
  application:
    name: ccpc-backend

  profiles:
    active: dev

  devtools:
    restart:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    # properties:
    #   hibernate:
    #     dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 500MB

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml

# 短信服务
sms:
  send-url: https://smssh1.253.com/msg/v1/send/json
  account: N8230425
  password: 5oUEwxTl1
